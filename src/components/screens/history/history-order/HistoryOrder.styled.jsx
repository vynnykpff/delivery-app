import styled from "@emotion/styled";

const OrderInfoList = styled.ul`
  list-style: none;
  font-size: 20px;
  color: var(--text-color);
  letter-spacing: 1.5px;
`;

const OrderInfoItem = styled.li`
  font-weight: 600;
`;

const OrderInfo = styled.h3`
  font-size: 22px;
  letter-spacing: 1.5px;
`;

export {OrderInfoItem, OrderInfoList, OrderInfo};